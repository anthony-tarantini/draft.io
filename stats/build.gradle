jar {
    baseName = 'magic-drafter-stats'
    version =  '0.0.1'
}

build.dependsOn clean

dependencies {
    compile project(':domain')
    compile('org.springframework.boot:spring-boot-starter-data-mongodb')
}